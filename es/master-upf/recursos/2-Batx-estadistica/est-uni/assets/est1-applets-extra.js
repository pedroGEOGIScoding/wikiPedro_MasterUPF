/* Módulo complementario · Estadística unidimensional.
   Reservado para histogramas SVG, sectores, polígono y boxplot avanzados.
   Se carga después del núcleo para mantener estable window.EST1. */
(function(){'use strict';if(!window.EST1)return;window.EST1.extra=true;})();